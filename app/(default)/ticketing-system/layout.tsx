export const metadata = {
  title: "Ticketing System | Modern IT/Ops Ticketing with SLAs & Automation - Cordoba AI",
  description: "Incident, service request, and change workflows with SLAs, escalations, knowledge base, analytics, and MS Teams/email integration.",
};

export default function TicketingSystemLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
