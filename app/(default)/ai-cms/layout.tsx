export const metadata = {
  title: "AI CMS | Website Builder with AI Agent (No Coding) - Cordoba AI",
  description: "Create and publish pages using an AI agent—no development. Component library, themes, approvals, SEO, and instant publishing.",
};

export default function AICMSLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
